---
name: Dashboard Metric Logging (Deprecated)
description: "DEPRECATED: Use the pin_to_dashboard tool instead. See skill: pin-to-dashboard."
trigger: log_metric
---

# Deprecated

The `log_metric` tool has been replaced by `pin_to_dashboard`. Use the **pin-to-dashboard** skill instead, which supports rich artifact types (tables, images, links, reports) in addition to simple numbers.
